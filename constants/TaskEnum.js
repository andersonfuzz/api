const TaskStatus = Object.freeze({
    DONE: "done",
    PENDING: "pending"
})
const TaskPriority = Object.freeze({
    LOW: "low",
    NORMAL: "normal",
    HIGH: "higt"
})

export {
    TaskPriority,
    TaskStatus
}