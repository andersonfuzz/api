const TaskStatus = Object.freeze({
    DONE: "done",
    PENDING: "pending"
})
const TaskPriority = Object.freeze({
    LOW: "low",
    NORMAL: "normal",
    HIGH: "high"
})

export {
    TaskPriority,
    TaskStatus
}